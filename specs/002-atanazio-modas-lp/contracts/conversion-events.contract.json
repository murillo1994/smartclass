{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ConversionEventContract",
  "description": "Contrato de eventos disparados para o dataLayer e Meta Pixel conforme Princípio VI da Constituição",
  "type": "object",
  "required": ["event", "category", "action", "label", "source_id", "timestamp"],
  "properties": {
    "event": {
      "type": "string",
      "enum": ["conversion_intent", "interaction"]
    },
    "category": {
      "type": "string",
      "enum": ["WhatsApp", "FAQ", "Navigation", "Location"]
    },
    "action": {
      "type": "string",
      "enum": ["click", "toggle", "scroll"]
    },
    "label": {
      "type": "string",
      "description": "Nome semântico do elemento clicado (ex: lead_whatsapp_hero)"
    },
    "source_id": {
      "type": "string",
      "description": "ID HTML único do botão ou elemento"
    },
    "timestamp": {
      "type": "integer",
      "description": "Timestamp Unix em milissegundos"
    },
    "page_location": {
      "type": "string",
      "description": "URL completa do acesso com parâmetros UTM"
    }
  }
}
